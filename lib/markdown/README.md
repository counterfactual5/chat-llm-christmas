# lib/markdown

| Module | Responsibility |
|--------|----------------|
| `math/` | Math-aware markdown helpers (public barrel: `@/lib/markdown/math`) — see `math/README.md` |
| `document-fence.ts` | Unwrap a whole-document ` ```markdown ` fence |
| `breaks.tsx` | Soft line-break handling for streamed markdown |
